/** URL da arte oficial (hotlink da digimoncard.io — fan-made, não redistribuímos). */
export const cardImageUrl = (number: string): string =>
  `https://images.digimoncard.io/images/cards/${number}.jpg`;
